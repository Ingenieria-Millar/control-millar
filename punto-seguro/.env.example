# ============================================
# PUNTO SEGURO — Variables de entorno (raíz)
# Copia este archivo como .env en /server y /client
# y completa los valores reales. Nunca subas el .env real a Git.
# ============================================

# Ver server/.env.example para las variables del backend
# Ver client/.env.example para las variables del frontend
